#include "DimShmProxy.h"
#include <unistd.h>
#include <stdint.h>

int main()
{
  DimShmProxy* c=new DimShmProxy();

  //s->scandns();
  //getchar();
  //s->scan();
  //getchar();
  //s->initialise();
  while (true)
    sleep((unsigned int) 3);
}
