#include "DimJobControl.h"
#include <unistd.h>
#include <stdint.h>

int main()
{
  DimJobControl* c=new DimJobControl();

  //s->scandns();
  //getchar();
  //s->scan();
  //getchar();
  //s->initialise();
  while (true)
    sleep((unsigned int) 3);
}
